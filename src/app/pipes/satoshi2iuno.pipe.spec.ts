import { Satoshi2IunoPipe } from './satoshi2iuno.pipe';

describe('Satoshi2IunoPipe', () => {
  it('create an instance', () => {
    const pipe = new Satoshi2IunoPipe();
    expect(pipe).toBeTruthy();
  });
});
